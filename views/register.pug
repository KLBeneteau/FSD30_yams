extends layouts/frontend

block content
    .body
        h1.vertClair Inscription rapide
        form(method="post", action="/login/register/").form
            div.flux
                div.form-input
                    label(for='email')
                        img(src=imgPath+'/user.png')
                    input#email(type='email', placeholder='Email', name='email', required)
                div.form-input
                    img(src=imgPath+'/lock.png').hidden
                    input#firstName(type='text', placeholder="Prénom", name='firstName', required)
                div.form-input
                    img(src=imgPath+'/lock.png').hidden
                    input#lastName(type='text', placeholder="Nom",name='lastName', required)
                div.form-input
                    label(for='password')
                        img(src=imgPath+'/lock.png')
                    input#password(type='password', placeholder='Mot de passe', name='password', required)
                div.form-input
                    label(for='confirm_password')
                        img(src=imgPath+'/lock.png')
                    input#confirm_password(type='password', placeholder='Confirmation', name='confirm_password', required)
            button(type="submit") S'enregistrer
            